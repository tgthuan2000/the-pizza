import styled from 'styled-components';
import { slideShow2 } from '../../animation';

export const CartInfoWrapper = styled.div`
	background-color: white;
	min-width: 300px;
	border-radius: 15px;
	border: 2px solid #e5e5e5;
	animation: ${slideShow2} ease 0.5s;
`;
